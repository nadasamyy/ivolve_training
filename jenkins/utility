@Library('jenkins-shared-library') _

pipeline {
    agent any
    stages {
        stage('Print Message') {
            steps {
                script {
                    utility.printMessage('This is a reusable function from the shared library.')
                }
            }
        }
    }
}
